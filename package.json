{
  "name": "ayazmo-plugin-comments",
  "version": "0.1.12",
  "description": "ayazmo plugin comments",
  "exports": "./dist/index.js",
  "author": "ayazmo.io",
  "license": "MIT",
  "rootDir": "src",
  "type": "module",
  "keywords": [
    "ayazmo-plugin-comments"
  ],
  "files": [
    "dist"
  ],
  "dependencies": {
    "@ayazmo/core": "^0.2.4",
    "@ayazmo/types": "^0.2.4",
    "@ayazmo/utils": "^0.2.4",
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.7",
    "fastify": "^4.26.2"
  },
  "scripts": {
    "build": "rimraf dist && tsc --build",
    "watch": "tsc --watch",
    "version": "changeset version",
    "publish": "changeset publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.2"
  }
}
